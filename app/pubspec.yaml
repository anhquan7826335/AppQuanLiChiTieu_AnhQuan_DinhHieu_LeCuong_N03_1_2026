name: appquanlichitieu
description: Quan ly chi tieu ca nhan (Flutter Web)
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.5.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  uuid: ^4.4.0
  intl: ^0.19.0
  collection: ^1.18.0
  csv: ^6.0.0
  universal_html: ^2.2.4
  google_fonts: ^6.2.1
  image_picker: ^1.0.7
  fl_chart: ^0.69.0
  path_provider: ^2.1.3
  path: ^1.9.0
  open_filex: ^4.5.0
  http: ^1.2.2
  shared_preferences: ^2.2.3

  # ✅ Firebase (thay thế Appwrite)
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true